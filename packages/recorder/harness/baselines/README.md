# Headroom baselines

Frozen premerge `credibility_b_report` used by later headroom slices to prove before/after deltas.

- `credibility-b-headroom.json` — S1/S2 × concurrency `[50,100]`, 10s warmup / 30s measure, 3 trials
