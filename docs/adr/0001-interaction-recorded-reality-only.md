# Interaction is recorded reality only

The Interaction artifact records what happened during one HTTP-inbound execution. Derived product classifications, policies, and alerting workflows are outside the artifact and outside this repository. Baking product policy into the manifest was rejected because it couples changing policy to a portable immutable artifact and breaks the immutability/portability story.
