{
  "name": "@epok/demo",
  "version": "0.0.0",
  "private": true,
  "description": "Unpublished in-repo demo for the Epok golden path",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "golden": "node dist/golden-path.js"
  },
  "dependencies": {
    "@epok/cli": "workspace:*",
    "@epok/core": "workspace:*",
    "@epok/recorder": "workspace:*",
    "@epok/storage-fs": "workspace:*"
  },
  "engines": {
    "node": ">=22"
  }
}
